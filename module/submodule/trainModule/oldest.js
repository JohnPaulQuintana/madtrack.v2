module.exports = function addOldest(manager){
    manager.addDocument("en", "What are the oldest product?", "asking.products.oldest");
    manager.addDocument("en", "how many oldest products do we have?", "asking.products.oldest");
   
}